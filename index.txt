<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Undangan Pernikahan - Romeo & Juliet</title>
    
    <!-- Google Fonts: Playfair Display (Serif Elegant) & Montserrat (Sans Clean) -->
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,300;0,400;0,600;1,300&family=Playfair+Display:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
    
    <!-- AOS CSS -->
    <link rel="stylesheet" href="https://unpkg.com/aos@next/dist/aos.css" />

    <style>
        :root {
            --bg-color: #0b1325; /* Dark Navy / Charcoal */
            --card-bg: #141e33;
            --accent-gold: #c5a059; /* Soft Champagne Gold */
            --text-light: #f4f4f6;
            --text-muted: #a0aab8;
            --font-serif: 'Playfair Display', Georgia, serif;
            --font-sans: 'Montserrat', sans-serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-sans);
            background-color: var(--bg-color);
            color: var(--text-light);
            text-align: center;
            overflow-x: hidden;
            letter-spacing: 0.5px;
        }

        /* Cover Section */
        header {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background: linear-gradient(180deg, rgba(11,19,37,0.4) 0%, rgba(11,19,37,0.95) 100%), 
                        url('https://images.unsplash.com/photo-1519741497674-611481863552?auto=format&fit=crop&w=1600&q=80') center/cover no-repeat;
            padding: 20px;
        }

        .subtitle {
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 4px;
            color: var(--accent-gold);
            margin-bottom: 15px;
        }

        .title {
            font-family: var(--font-serif);
            font-size: 3.5rem;
            font-weight: 400;
            color: var(--text-light);
            line-height: 1.2;
            margin-bottom: 15px;
        }

        .date-badge {
            font-size: 0.9rem;
            letter-spacing: 2px;
            color: var(--text-muted);
            border-top: 1px solid var(--accent-gold);
            border-bottom: 1px solid var(--accent-gold);
            padding: 8px 25px;
            margin-top: 10px;
        }

        /* Section Layout */
        .section {
            padding: 90px 20px;
            max-width: 750px;
            margin: 0 auto;
        }

        .section-title {
            font-family: var(--font-serif);
            font-size: 2rem;
            color: var(--accent-gold);
            margin-bottom: 40px;
            position: relative;
        }

        .section-title::after {
            content: '';
            display: block;
            width: 40px;
            height: 1px;
            background: var(--accent-gold);
            margin: 15px auto 0;
        }

        /* Card Styling */
        .card {
            background: var(--card-bg);
            border: 1px solid rgba(197, 160, 89, 0.2);
            padding: 40px 30px;
            border-radius: 4px; /* Minimalist square border */
            box-shadow: 0 15px 35px rgba(0,0,0,0.4);
        }

        .couple-name {
            font-family: var(--font-serif);
            font-size: 2.2rem;
            color: var(--text-light);
            margin-bottom: 5px;
        }

        .parents {
            font-size: 0.85rem;
            color: var(--text-muted);
            font-weight: 300;
        }

        .ampersand {
            font-family: var(--font-serif);
            font-size: 2rem;
            color: var(--accent-gold);
            margin: 25px 0;
            font-style: italic;
        }

        /* Event Details */
        .event-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
            margin-top: 20px;
        }

        @media (max-width: 600px) {
            .event-grid { grid-template-columns: 1fr; }
            .title { font-size: 2.5rem; }
        }

        .event-box {
            background: var(--card-bg);
            border: 1px solid rgba(197, 160, 89, 0.2);
            padding: 30px 20px;
        }

        .event-type {
            font-family: var(--font-serif);
            color: var(--accent-gold);
            font-size: 1.3rem;
            margin-bottom: 10px;
        }

        .event-time {
            font-size: 0.85rem;
            color: var(--text-muted);
            margin-bottom: 15px;
        }

        /* Gold Elegant Button */
        .btn-gold {
            display: inline-block;
            margin-top: 25px;
            padding: 12px 35px;
            border: 1px solid var(--accent-gold);
            color: var(--accent-gold);
            text-decoration: none;
            font-size: 0.8rem;
            letter-spacing: 2px;
            text-transform: uppercase;
            transition: all 0.4s ease;
            background: transparent;
        }

        .btn-gold:hover {
            background: var(--accent-gold);
            color: var(--bg-color);
            box-shadow: 0 0 15px rgba(197, 160, 89, 0.4);
        }

        footer {
            padding: 40px 20px;
            background: #070c18;
            font-size: 0.8rem;
            color: var(--text-muted);
            border-top: 1px solid rgba(197, 160, 89, 0.1);
        }
    </style>
</head>
<body>

    <!-- Cover Section -->
    <header>
        <p class="subtitle" data-aos="fade-down" data-aos-duration="1000">The Wedding Of</p>
        <h1 class="title" data-aos="zoom-in" data-aos-duration="1200">Romeo & Juliet</h1>
        <div class="date-badge" data-aos="fade-up" data-aos-duration="1000">12 . 10 . 2026</div>
    </header>

    <!-- Mempelai -->
    <section class="section">
        <h2 class="section-title" data-aos="fade-up">Mempelai</h2>
        <div class="card" data-aos="fade-up" data-aos-delay="200">
            <h3 class="couple-name">Juliet Capulet</h3>
            <p class="parents">Putri dari Bapak Capulet & Ibu Capulet</p>
            
            <div class="ampersand">&</div>
            
            <h3 class="couple-name">Romeo Montague</h3>
            <p class="parents">Putra dari Bapak Montague & Ibu Montague</p>
        </div>
    </section>

    <!-- Acara -->
    <section class="section">
        <h2 class="section-title" data-aos="fade-up">Rangkaian Acara</h2>
        <div class="event-grid">
            <div class="event-box" data-aos="fade-right" data-aos-delay="200">
                <h3 class="event-type">Akad Nikah</h3>
                <p class="event-time">Sabtu, 12 Oktober 2026<br>08.00 - 10.00 WIB</p>
                <p style="font-size:0.85rem;"><strong>Gedung Grand Ballroom</strong><br>Jl. Mawar No. 123, Jakarta</p>
            </div>
            <div class="event-box" data-aos="fade-left" data-aos-delay="400">
                <h3 class="event-type">Resepsi</h3>
                <p class="event-time">Sabtu, 12 Oktober 2026<br>11.00 - 14.00 WIB</p>
                <p style="font-size:0.85rem;"><strong>Gedung Grand Ballroom</strong><br>Jl. Mawar No. 123, Jakarta</p>
            </div>
        </div>
        
        <div data-aos="fade-up" data-aos-delay="500">
            <a href="https://maps.google.com" target="_blank" class="btn-gold">Petunjuk Lokasi</a>
        </div>
    </section>

    <footer>
        <p>Merupakan suatu kehormatan bagi kami atas kehadiran & doa restu Anda.</p>
    </footer>

    <!-- AOS JS -->
    <script src="https://unpkg.com/aos@next/dist/aos.js"></script>
    <script>
        AOS.init({
            once: true,
            duration: 1000,
            easing: 'ease-in-out'
        });
    </script>
</body>
</html>
